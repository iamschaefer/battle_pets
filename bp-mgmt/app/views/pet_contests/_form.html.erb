<%= form_for(pet_contest) do |f| %>
    <% if pet_contest.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(pet_contest.errors.count, "error") %> prohibited this pet_contest from being saved:</h2>

          <ul>
            <% pet_contest.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :contest_type %>
      <%= f.text_field :contest_type %>
    </div>

    <div class="field">
      <%= f.label :challenger %>
      <%= f.text_field :challenger_id %>
    </div>

    <div class="field">
      <%= f.label :challenged %>
      <%= f.text_field :challenged_id %>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
